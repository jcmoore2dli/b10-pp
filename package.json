{
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "mocha": "^11.7.5"
  },
  "scripts": {
    "test": "mocha test/firestore.rules.test.js --timeout 10000",
    "test:lar": "mocha \"test/lar/*.test.js\"",
    "test:toefl-accounts": "firebase emulators:exec --only auth,firestore --project demo-toefl-accounts \"mocha test/toeflAccounts.test.js --timeout 15000\""
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "firebase-admin": "^13.8.0"
  }
}
